Process Oriented: Code acting on data.
Object Oriented: Data is used to create code.[behaviour of data]

Platform Independent Pragramming Language
   Reason: because its source code is compiled into an intermediate format called bytecode, rather than direct machine code. This means you can "Write Once, Run Anywhere" (WORA).

bytecode ---> JRE(JVM) ---> execute

syntax:
  class class_name
  {
    public static void main(String args[])
    { 
        ---- content ----
    }
  }
